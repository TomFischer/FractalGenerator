# source files
GET_SOURCE_FILES( SOURCES_GUI )
SET ( SOURCES ${SOURCES_GUI} )

INCLUDE_DIRECTORIES (
        .
        ${GTKMM_INCLUDE_DIRS}
)


ADD_LIBRARY (GUI STATIC ${SOURCES} )

SET_TARGET_PROPERTIES(GUI PROPERTIES LINKER_LANGUAGE CXX)
